<% title="Songs By Sinatra" %>
<!doctype html>
<html lang="en">
<head>
	<title><%= @title %></title>
	<meta charset="utf-8">
	<%= css :styles %>
</head>
<body>
	<header>
		<h1>Songs By Sinatra</h1>
	</header>
	<%= erb :nav %>
	<section>
	  <%= styled_flash %>
		<%= yield %>
	</section>
	<footer>
		<% if authorized? %>
			<a href="/logout">Log Out</a>
		<% else %>
			<a href="/login">Log In</a>
		<% end %>
	</footer>
</body>
</html>
